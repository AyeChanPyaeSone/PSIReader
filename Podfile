# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PSIReader' do
    pod 'Realm'
    pod 'Obfuscator', '~> 2.0'
    pod 'CustomIOSAlertView'
  target 'PSIReaderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PSIReaderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
